# Per-user workspace — created locally by bootstrap/init.sh, never committed to the harness repo
/inputs/
/package/
/personal/
/preview/
/reports/
/dist/
/guide/
/.coursewerk/

# Saved example builds (e.g. examples/openstax_chemistry/) — kept locally for reference, never committed
/examples/
node_modules/
package-lock.json
.DS_Store
